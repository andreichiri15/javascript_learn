export default function Burger({toggleMenu, isOpened}) {
    return (
        <div onClick={toggleMenu} className="burger">
            <div/>
            <div/>
            <div/>
        </div>
    )
}
